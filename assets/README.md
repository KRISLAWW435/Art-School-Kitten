# Assets Folder
Upload your images here. You can then use them in the code or upload them to your GitHub repository.

Currently, the application uses direct GitHub raw URLs for:
- Background: `bg.png`
- Randy the Cat: `cat.png`
